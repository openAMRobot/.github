name: Repository quality

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  repository-quality:
    name: repository-quality
    uses: ./.github/workflows/repository-quality-reusable.yml
